{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Spec §9.5: mutation score >= 80% for the rules and the policy. Line coverage says a line ran; mutation score says a test would have noticed if it were wrong, which is the only meaningful question for a security rule.",
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vitest.config.ts"
  },
  "reporters": [
    "progress",
    "clear-text"
  ],
  "coverageAnalysis": "perTest",
  "mutate": [
    "src/domain/rules/**/*.ts",
    "src/domain/policy/**/*.ts",
    "!src/domain/**/index.ts"
  ],
  "thresholds": {
    "high": 90,
    "low": 80,
    "break": 80
  },
  "timeoutMS": 20000,
  "concurrency": 4
}
